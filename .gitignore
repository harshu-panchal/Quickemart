node_modules/
.env

# Locally-stored uploads (products/categories/etc.) — persisted on the
# server outside the deploy directory, not part of the repo.
backend/public/uploads/

logs/
*.log

coverage/
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
